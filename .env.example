# Public Google OAuth Web client ID, used only by the browser button.
VITE_GOOGLE_CLIENT_ID=
# Browser development uses /api through Vite's proxy. For Android, set a full API URL before android:sync.
VITE_API_URL=/api
